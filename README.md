These files will allow you to query a database and find duplicates or
near duplicates within the database. You can use the fuzzywuzzy
library, a tdif vectorizer, or just a plain cosine similarity method.
